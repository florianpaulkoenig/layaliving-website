export function Pull() {
  return (
    <section className="pull">
      <blockquote>
        From sunrise on <span className="it">Rigi</span> to sunset over the{" "}
        <span className="it">Pilatus</span> ridge — the full panorama, from
        your couch.
      </blockquote>
    </section>
  );
}
